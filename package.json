{
  "name": "museum-project",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "^0.166.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.2",
    "vite": "^6.0.0",
    "wrangler": "^4.100.0"
  }
}
